@model MakerFarm.Models.MaterialCheckout

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>


@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>MaterialCheckout</h4>
        <hr />
        @Html.ValidationSummary(true)
        @Html.HiddenFor(model => model.MaterialCheckoutId)

        <div class="form-group">
            @Html.LabelFor(model => model.PrinterId, "PrinterId", new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.DropDownList("PrinterId", String.Empty)
                @Html.ValidationMessageFor(model => model.PrinterId)
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.MaterialId, "MaterialId", new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.DropDownList("MaterialId", String.Empty)
                @Html.ValidationMessageFor(model => model.MaterialId)
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
