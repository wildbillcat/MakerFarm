@model IEnumerable<MakerFarm.Models.MaterialCheckout>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Material.MaterialName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Printer.PrinterName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Material.MaterialName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Printer.PrinterName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MaterialCheckoutId }) |
            @Html.ActionLink("Details", "Details", new { id=item.MaterialCheckoutId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MaterialCheckoutId })
        </td>
    </tr>
}

</table>
